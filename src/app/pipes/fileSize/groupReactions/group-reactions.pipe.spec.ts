import { GroupReactionsPipe } from './group-reactions.pipe';

describe('GroupReactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupReactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
